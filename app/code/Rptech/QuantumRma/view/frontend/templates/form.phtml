<?php
/**
 * @var \Rptech\QuantumRma\Block\Form $block
 */
?>
<!-- Bootstrap CSS -->
<!--<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">-->

<!-- Google Fonts -->
<link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;500;700&display=swap" rel="stylesheet">

<style>
    body {
        font-family: 'Roboto', sans-serif;
        background-color: #f5f7fa;
        margin: 0;
        padding: 20px;
    }
    .form-section {
        margin-bottom: 30px;
    }
    .form-section h5 {
        background-color: #003366;
        color: white;
        padding: 10px 15px;
        border-radius: 4px;
        margin-bottom: 20px;
    }
    .card {
        border: none;
        border-radius: 12px;
        box-shadow: 0px 4px 12px rgba(0,0,0,0.1);
        padding: 25px;
    }
    label {
        font-weight: 500;
        margin-bottom: 5px;
    }
    .form-control:focus {
        box-shadow: none;
        border-color: #003366;
    }
    .btn-primary {
        background-color: #003366;
        border-color: #003366;
        border-radius: 6px;
        font-weight: 500;
    }
    .btn-primary:hover {
        background-color: #00509e;
        border-color: #00509e;
    }
    .is-invalid {
        border-color: #dc3545 !important;
    }
    .form-control.is-invalid, .was-validated .form-control:invalid {
        padding-right: calc(1.5em + .75rem);
        background-image: url("data:image/svg+xml,%3csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 12 12' width='12' height='12' fill='none' stroke='%23dc3545'%3e%3ccircle cx='6' cy='6' r='4.5'/%3e%3cpath stroke-linejoin='round' d='M5.8 3.6h.4L6 6.5z'/%3e%3ccircle cx='6' cy='8.2' r='.6' fill='%23dc3545' stroke='none'/%3e%3c/svg%3e");
        background-repeat: no-repeat;
        background-position: right calc(.375em + .1875rem) center;
        background-size: calc(.75em + .375rem) calc(.75em + .375rem);
    }
</style>
</head>
<body>

<div class="container">
    <?php if ($message = $block->getSuccessMessage()):  ?>
    <div class="alert alert-success">
        <strong>Success!</strong> <?= $message; ?>
    </div>
    <?php endif; ?>
    <form action="<?= $block->getActionUrl(); ?>" method="post" id="quantumrma-form">
        <?= $block->getBlockHtml('formkey') ?>
        <?php
        echo $this->getLayout()->createBlock('Magento\Cms\Block\Block')
                ->setBlockId('quantum_rma')
                ->toHtml();
        ?>
    </form>
</div>

<!-- JavaScript Validation -->
<script>
    function validateForm() {
        console.log("I am herer");
        const inputs = document.querySelectorAll('#quantumrma-form .form-control[required], #quantumrma-form textarea[required]');
        let valid = true;

        inputs.forEach(input => {
            console.log("input value here => "+input.value);
            if (input.value.trim() === '') {
                input.classList.add('is-invalid');
                valid = false;
            } else {
                input.classList.remove('is-invalid');
            }
        });
        console.log(valid);
        if (valid) {
            document.getElementById('quantumrma-form').submit();
        }
    }
</script>


